The lines below run simulations of a single disordered RNA.

```bash
python prepare.py 
python <protein name>/run.py 
```

Note that `<RNA name>` needs to be listed in the corresponding fasta file rna.fasta.
